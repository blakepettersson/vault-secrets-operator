# Copyright (c) HashiCorp, Inc.
# SPDX-License-Identifier: MPL-2.0

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: vaultpolicies.secrets.hashicorp.com
spec:
  group: secrets.hashicorp.com
  names:
    kind: VaultPolicy
    listKind: VaultPolicyList
    plural: vaultpolicies
    singular: vaultpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VaultPolicy is the Schema for the vaultmounts API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: VaultPolicySpec defines the desired state of VaultPolicySpec
            properties:
              namespace:
                description: Namespace to auth to in Vault
                type: string
              path:
                additionalProperties:
                  properties:
                    allowedParameters:
                      additionalProperties:
                        items:
                          type: string
                        type: array
                      type: object
                    capabilities:
                      items:
                        type: string
                      type: array
                    deniedParameters:
                      additionalProperties:
                        items:
                          type: string
                        type: array
                      type: object
                    maxWrappingTTL:
                      type: string
                    minWrappingTTL:
                      type: string
                    requiredParameters:
                      items:
                        type: string
                      type: array
                  type: object
                description: Mount to use when authenticating to auth method.
                type: object
              vaultAuthRef:
                description: VaultAuthRef of the VaultAuth resource If no value is
                  specified the Operator will default to the `default` VaultAuth,
                  configured in its own Kubernetes namespace.
                type: string
            required:
            - path
            type: object
          status:
            description: VaultPolicyStatus defines the observed state of VaultPolicySpec
            properties:
              error:
                type: string
              name:
                type: string
              valid:
                description: Valid auth mechanism.
                type: boolean
            required:
            - error
            - name
            - valid
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
